{
    // 项目名称(package包名), 注意 mappings 的 dir 的值
	"project" 			: "com.company.project",
    // 如果这里不进行设置的话, 注意模板里有些地方需要去掉
	"alias" 			: "myProject",
	"author" 			: "xuyl",

    // 数据库信息. 目前仅支持 mysql 或 oracle
	"dbConfig" : {
		"driverClass" 	: "org.gjt.mm.mysql.Driver",
		"username" 		: "root",
		"password" 		: "xu3352",
		"dbName" 		: "mars",
		"url" 			: "jdbc:mysql://localhost:3306/mars?useUnicode=true&characterEncoding=UTF-8"
	},

    // 表明截断前缀, 如: t_test_table 生成 POJO 文件名为:TestTable
	"ignorePrefix" : "t_",

    // 多个表可以放到同一个组的目录下面, 否则以表名为模块创建对应的文件夹
	"groups" : [{
			"name" 	 : "course",
			"prefix" : "course,question,quiz"
		}, {
			"name" 	 : "system",
			"prefix" : "sys"
		}, {
			"name" 	 : "users",
			"prefix" : "user"
    }],

    // 自定义模板根目录
	"templateDir" : "resources/template/ssm_cms/",

    // 每个自定义模板的 路径和文件名 生成规则
	"mappings" : [{
			"template"	:	"action.ftl",
			"dir"		:	"${project}/web/${alias}",
			"rpadding"	:	"Action"
		}, {
			"template"	:	"pojo.ftl",
			"dir"		:	"${project}/pojo/${alias}"
		}, {
			"template"	:	"page.ftl",
			"dir"		:	"${project}/page/${alias}",
			"rpadding"	:	"Page"
		}, {
			"template"	:	"service.ftl",
			"dir"		:	"${project}/service/${alias}",
			"rpadding"	:	"Service"
		}, {
			"template"	:	"service_impl.ftl",
			"dir"		:	"${project}/service/${alias}/impl",
			"rpadding"	:	"ServiceImpl"
		}, {
			"template"	:	"dao.ftl",
			"dir"		:	"${project}/dao/${alias}",
			"rpadding"	:	"Dao"
		}, {
			"template"	:	"dao_impl.ftl",
			"dir"		:	"${project}/dao/${alias}/impl",
			"rpadding"	:	"DaoImpl"
		}, {
			"template"	:	"mybatis.ftl",
			"dir"		:	"sqlmap/${alias}",
            "rpadding"	:	"Dao",
			"suffix"	:	"xml"
		}]
}